https://www.youtube.com/watch?v=PMfcsYzj-9M#t=730

the 2 ways to create an object with protypal assign:
  Creating objects using the more common constructor pattern
  Creating objects using the newer Object.create syntax

// Option 1: The Hacky Way
myObj.__proto__ = myParentObj

// Option 2: The Less Hacky Way
Object.setPrototypeOf( myObj, myParentObj )

~~

// Examine the prototype in a more "official" way
Object.getPrototypeOf(childObj);
//=> Object {howdy: "neighbor"}

// Set a new prototype the more "official" way
Object.setPrototypeOf(childObj, someOtherObj);

~~


// The prototype of an anonymous object:
var foo = { a: "b" }
foo.__proto__
//=> Object {}


// http://diveintohtml5.info/canvas.html

var c_canvas = document.getElementById("c");
var context = c_canvas.getContext("2d");

// context.arc(x, y, radius, 0, Math.PI * 2);
//sizes: 20, 30, 40

context.arc(200, 100, 40, 0, Math.PI * 2);



context.fill();
context.stroke();


// To draw straight lines in pencil, you use the following two methods:
// moveTo(x, y) moves the pencil to the specified starting point.
// lineTo(x, y) draws a line to the specified ending point.

//

//  to make it permanent.
// context.strokeStyle = "#eee";
// context.stroke();

//


//to draw a new path, use beginPath();
// context.beginPath();
// context.moveTo(0, 40);
// context.lineTo(240, 40);

//
